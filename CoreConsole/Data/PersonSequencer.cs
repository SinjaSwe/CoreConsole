using System;
using System.Collections.Generic;
using System.Text;

namespace CoreConsole.Data
{
    class PersonSequencer
    {
        /*
        a.In PersonSequencer createsa private static int variable called personId.
        b.Add a static method called nextPersonId that increment and return the next personId value.
        c.Add a static method called reset() that sets the personId variable to 0.
        d.Unit test PersonSequencer.e.Commitchanges.
        */
    }
}
