using System;
using System.Collections.Generic;
using System.Text;

namespace CoreConsole.Data
{
    class TodoItems
    {
        /*
        a.TodoItems should have the same functionality as the People class.
        b.Unit test TodoItems class
        c.Commit changes
        */


        /*
         a.publicTodo[] FindByDoneStatus(booldoneStatus)–Returns array with objects that has a matching done status.
        b.public Todo[] FindByAssignee(int personId)–Returns array with objects that has an assignee with a personId matching.
        c.public Todo[] FindByAssignee(Person assignee)–Returnsarray with objects that has sent in Person.
        d.public Todo[] FindUnassignedTodoItems()–Returns an array of objects that does not have an assignee set.e.Unit test changesf.Commit.
        */
    }
}
